---
// ColorBends-style animated gradient — rotation 0, speed 0.2, colors #c70088 #affea4 #c7ff2e #001ba3, scale 1.1
// Pure CSS, no React; full-viewport background
---
<div class="color-bends" aria-hidden="true">
  <div class="color-bends__gradient" />
</div>

<style>
  .color-bends {
    position: fixed;
    inset: 0;
    z-index: 0;
    overflow: hidden;
  }
  .color-bends__gradient {
    width: 165%;
    height: 165%;
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%) scale(1.1) rotate(0deg);
    animation: color-bends-spin 90s linear infinite;
    background: conic-gradient(
      from 0deg at 50% 50%,
      #c70088 0deg,
      #affea4 90deg,
      #c7ff2e 180deg,
      #001ba3 270deg,
      #c70088 360deg
    );
    filter: blur(80px);
    opacity: 1;
  }
  @keyframes color-bends-spin {
    to {
      transform: translate(-50%, -50%) scale(1.1) rotate(360deg);
    }
  }
</style>
